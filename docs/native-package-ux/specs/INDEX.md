# Native-Package-UX — Implementation Specs index

| # | Eff | Idea | Deps | API/schema | BE | FE |
|---|---|---|---|---|---|---|
| #1 | S | [Per-ecosystem usage emitter: kill docker pull for npm/maven](01-per-ecosystem-usage-emitter-kill-docker-pull-for.md) | none | none — extra_attrs is already returned b… | 0 | 5 |
| #2 | M | [Readable native identity everywhere (decode display name + format badge/icons), storage path hidden](02-readable-native-identity-everywhere-decode-displ.md) | none (this idea is the precondition for #1/#3/#4/#5/#8; nothing it depends on) | none. No swagger.yaml edit (so no task b… | 5 | 7 |
| #3 | M | [Per-ecosystem Install/Usage ADDITIONS tab (full setup snippet, scoped to artifact type)](03-per-ecosystem-install-usage-additions-tab-full-s.md) | Soft-depends on #1 for the hasPullCommand=false edit (so docker pull does not also show). Functionally standalone otherwise; the ArtifactType.NPM/MAVEN enum additions are shared with #1/#2/#4 (whichever lands first adds them). | none. No swagger.yaml change, no make/mi… | 0 | 11 |
| #4 | S | [Native coordinate as version-list primary identity + channel/dist-tag badge](04-native-coordinate-as-version-list-primary-identi.md) | Independent of other ideas for its own correctness. Shares the ArtifactType enum extension (add NPM/MAVEN) with ideas #1 and #2; whichever lands first adds the enum members, the other reuses them. Does NOT require #2 (decode/icons) to function, but reads best alongside it. | none — no swagger.yaml edit, no task build… | 0 | 5 |
| #5 | M | [Search-by-native-name (query the readable coordinate, not the escaped path)](05-search-by-native-name-query-the-readable-coordin.md) | Soft-depends on Idea #2 (readable native identity) for UX coherence: search results must render decoded names or a match-with-garbled-name reads as a bug. #5 is functionally independent and can ship/verify via API before #2 lands, but should not ship to the portal UI before #2. | none. No swagger.yaml change (q already … | 2 | 1 |
| #6 | M | [Inline trust chips per version (reuse scan/SBOM/signature)](06-inline-trust-chips-per-version-reuse-scan-sbom-s.md) | #4 (native coordinate + channel badge) — soft dependency only. #6 reuses the same per-row extra_attrs binding #4 introduces and is most coherent shipped after #4, but #6 adds no code to #4's surface and works standalone. No hard code dependency. | none — zero swagger.yaml change, zero mi… | 0 | 5 |
| #7 | M | [Maven GAV Files tab (classifiers/checksums/snapshot-vs-release) via additions seam](07-maven-gav-files-tab-classifiers-checksums-snapsh.md) | none | none. swagger.yaml already lists 'files'… | 1 | 4 |
| #8 | M | [Project-level Packages view: ecosystem-grouped, per-format filter chips with counts](08-project-level-packages-view-ecosystem-grouped-pe.md) | #2 (readable native identity + icons). #8 reuses #2's frontend decode helper for the row label and #2's per-format type icon; without #2 the grouped rows would still show the escaped storage path. #8 adds no decode logic of its own. | none. GET /projects/{name}/repositories … | 0 | 5 |
| #9 | M | [npm README + Versions/packument additions (dist-tags + cross-version, from PG PackageState)](09-npm-readme-versions-packument-additions-dist-tag.md) | none | Swagger: add `versions` to the getAdditi… | 2 | 5 |
| #10 | S | [Deprecation / yank banner from existing multi_format_version.yanked fact](10-deprecation-yank-banner-from-existing-multi-format-versi.md) | Functionally depends on a yank-mutation path existing (a backend action/adapter that sets multi_format_version.yanked=true and the _index AnnYanked annotation to "true"). That path does NOT exist in the repo today (Publish hardcodes yanked=false at mapper.go:148; no controller/adapter writes yanked). This idea (#10) is purely the surfacing layer and is otherwise self-contained. If no yank action ships, the banner/badge are correct but never trigger on real data — verification then requires manually setting the fact (SQL UPDATE + _index re-push, or a temporary test stub). Treat the yank action as a separate idea/dependency. | none — no swagger.yaml change (extra_att… | 4 | 6 |
