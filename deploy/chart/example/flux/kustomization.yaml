apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # SOPS-encrypt before committing, or replace with ExternalSecrets /
  # SealedSecrets manifests.
  - identity-secrets.yaml
  - source.yaml
  - helmrelease.yaml
