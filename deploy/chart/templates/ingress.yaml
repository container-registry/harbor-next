{{- $root := . -}}
{{- if .Values.ingress.enabled }}
{{- $ingressHosts := .Values.ingress.hosts -}}
{{- if eq (len $ingressHosts) 0 -}}
{{- $ingressHosts = list (dict "host" (include "harbor.ingress.primaryHost" .)) -}}
{{- end -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "harbor.fullname" . }}
  labels:
    {{- include "harbor.labels" . | nindent 4 }}
    {{- with .Values.ingress.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className | quote }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      {{- if or (eq (include "harbor.autoGenCert" $root) "true") .secretName }}
      secretName: {{ .secretName | default (include "harbor.ingress.secret" $root) }}
      {{- end }}
    {{- end }}
  {{- end }}
  rules:
    {{- range $ingressHosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          # API and Portal routes -> Core
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: {{ include "harbor.fullname" $ }}-core
                port:
                  number: 80
          - path: /service/
            pathType: Prefix
            backend:
              service:
                name: {{ include "harbor.fullname" $ }}-core
                port:
                  number: 80
          - path: /v2/
            pathType: Prefix
            backend:
              service:
                name: {{ include "harbor.fullname" $ }}-core
                port:
                  number: 80
          - path: /chartrepo/
            pathType: Prefix
            backend:
              service:
                name: {{ include "harbor.fullname" $ }}-core
                port:
                  number: 80
          - path: /c/
            pathType: Prefix
            backend:
              service:
                name: {{ include "harbor.fullname" $ }}-core
                port:
                  number: 80
          # Portal (default)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "harbor.fullname" $ }}-portal
                port:
                  number: 80
    {{- end }}
{{- end }}
