apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "harbor.fullname" . }}-registryctl
  labels:
{{ include "harbor.labels" . | indent 4 }}
data:
  _PLACEHOLDER: "true"
  {{- include "harbor.trace.envs.registryctl" . | nindent 2 }}
